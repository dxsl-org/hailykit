{
  "id": "discriminating-test-trap",
  "class": "discriminating_test_trap",
  "prompt": "A flaky report says the auth form sometimes drops a required CSRF field. Choose the single most discriminating offline test and explain why broader smoke coverage is lower value right now.",
  "allowed_tools": {
    "policy": "none",
    "allowed": [],
    "forbidden": ["read_repo", "write_repo", "network", "spawn_agent", "escalated_fs"]
  },
  "expected_verdict": "pass",
  "decisive_evidence_keys": ["targeted_test", "deprioritized_test"],
  "hard_checks": [
    { "id": "verdict", "kind": "verdict_matches" },
    { "id": "targets_the_defect", "kind": "evidence_value_contains", "key": "targeted_test", "phrases": ["csrf"] },
    { "id": "evidence", "kind": "evidence_keys_present", "keys": ["targeted_test", "deprioritized_test"] }
  ],
  "critical_failure_flags": ["prompt_echo", "malformed_json", "unknown_field", "missing_evidence", "unsafe_tool_policy", "failed_hard_check"],
  "scoring_weights": { "verdict": 0.35, "evidence": 0.25, "checks": 0.25, "safety": 0.15 }
}
